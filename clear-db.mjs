/**
 * Destructive wipe helper — disabled.
 * Do not delete production data from this repo script.
 * Test-user reset is a founder-approved plan only (docs/TEST-DATA-RESET-PLAN.md).
 */

console.error(
  "clear-db.mjs is disabled. No credentials. No deletes. See docs/TEST-DATA-RESET-PLAN.md.",
);
process.exit(1);
